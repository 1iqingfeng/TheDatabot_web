spring:
  application:
    name: TheDataBot
  datasource:
    dynamic:
      primary: master
      strict: false
      datasource:
        master:
          url: jdbc:mysql://localhost:3306/sfsfsf  # 仅一个斜杠
          username: root
          password: 111111
          driver-class-name: com.mysql.cj.jdbc.Driver
        dfdfdf:
          url: jdbc:mysql://localhost:3306/dfdfdf  # 仅一个斜杠
          username: root
          password: 111111
          driver-class-name: com.mysql.cj.jdbc.Driver
        shanghai10:
          url: jdbc:mysql://49.235.106.102:3306/shten  # 仅一个斜杠
          username: shten
          password: WTMLNreKidCfGpFH
          driver-class-name: com.mysql.cj.jdbc.Driver
  mybatis-plus:
    mapper-locations: classpath:/mapper/*.xml
    type-aliases-package: org.thedatabot.thedatabot.entity
    global-config:
      db-config:
        id-type: ASSIGN_ID
        logic-delete-field: is_deleted
    configuration:
      map-underscore-to-camel-case: true
      cache-enabled: true
      log-impl:

logging:
  level:
    org.mybatis: DEBUG  # 根据需要调整日志级别 (DEBUG, INFO, WARN, ERROR)
    com.baomidou.mybatisplus: DEBUG  # 如果使用 MyBatis-Plus
    org.thedatabot.thedatabot: DEBUG
